#!/usr/bin/env fish

set absarg (echo 'import sys;import os;print(os.path.abspath(sys.argv[1]))'| python - $argv)

feh --bg-scale $absarg
gsettings set org.gnome.desktop.screensaver picture-uri "file://"$absarg
gsettings set org.gnome.desktop.background picture-uri "file://"$absarg

